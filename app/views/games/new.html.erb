<%= simple_form_for(@game) do |f| %>
  <%= f.input :name %>
  <%= simple_fields_for :other do |o| %>
    <%= o.input :sfw, as: :boolean, label: 'Play with safe for work cards' %>
  <% end %>
  <%= f.button :submit %>
<% end %>
